{
  "hash": "4d63243eec34a3423039c0decc4331a2",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: Linear Transformations\ndescription: Matrices as functions that move vectors while preserving linear structure.\nexecute:\n  echo: false\n---\n\nA **linear transformation** is a function that takes vectors as inputs and\nreturns vectors as outputs while preserving addition and scalar multiplication.\nIn symbols, a transformation $T$ is linear when\n\n$$\nT(\\mathbf{u}+\\mathbf{v})=T(\\mathbf{u})+T(\\mathbf{v})\n$$\n\nand\n\n$$\nT(c\\mathbf{v})=cT(\\mathbf{v}).\n$$\n\nMatrices are the most common way to represent linear transformations.\n\n## What you should be able to do\n\nBy the end of this lesson, you should be able to:\n\n- interpret a matrix as a function from vectors to vectors;\n- find the image of a vector under a matrix transformation;\n- explain why basis vectors determine the whole transformation;\n- identify common transformations: scaling, reflection, projection, and shear;\n- connect algebraic matrix products to geometric movement;\n- use edumath and SymPy to test transformations.\n\n## Matrices as functions\n\nIf\n\n$$\nA=\\begin{bmatrix}2&0\\\\0&3\\end{bmatrix},\n$$\n\nthen the rule $T(\\mathbf{x})=A\\mathbf{x}$ sends\n\n$$\n\\begin{bmatrix}x\\\\y\\end{bmatrix}\n\\mapsto\n\\begin{bmatrix}2x\\\\3y\\end{bmatrix}.\n$$\n\nSo this transformation stretches horizontal coordinates by 2 and vertical\ncoordinates by 3.\n\n## Basis vectors\n\nThe standard basis vectors in the plane are\n\n$$\n\\mathbf{e}_1=\\begin{bmatrix}1\\\\0\\end{bmatrix},\n\\qquad\n\\mathbf{e}_2=\\begin{bmatrix}0\\\\1\\end{bmatrix}.\n$$\n\nEvery vector $\\begin{bmatrix}x\\\\y\\end{bmatrix}$ can be written as\n\n$$\nx\\mathbf{e}_1+y\\mathbf{e}_2.\n$$\n\nBecause a linear transformation preserves combinations, once you know where\n$\\mathbf{e}_1$ and $\\mathbf{e}_2$ go, you know the transformation of every\nvector.\n\n::: {.callout-note}\n## Columns tell the story\n\nFor a $2\\times2$ matrix, the first column is where $\\mathbf{e}_1$ goes and the\nsecond column is where $\\mathbf{e}_2$ goes.\n:::\n\n## Common transformations\n\n### Scaling\n\n$$\n\\begin{bmatrix}2&0\\\\0&2\\end{bmatrix}\n$$\n\ndoubles every vector.\n\n### Reflection over the x-axis\n\n$$\n\\begin{bmatrix}1&0\\\\0&-1\\end{bmatrix}\n$$\n\nkeeps $x$ the same and flips the sign of $y$.\n\n### Projection onto the x-axis\n\n$$\n\\begin{bmatrix}1&0\\\\0&0\\end{bmatrix}\n$$\n\nsends $(x,y)$ to $(x,0)$.\n\n### Shear\n\n$$\n\\begin{bmatrix}1&1\\\\0&1\\end{bmatrix}\n$$\n\nsends $(x,y)$ to $(x+y,y)$. It slants the plane without changing horizontal\nlevels.\n\n## Worked example: identify a transformation\n\nLet\n\n$$\nA=\\begin{bmatrix}1&0\\\\0&-1\\end{bmatrix}.\n$$\n\nApply $A$ to a general vector:\n\n$$\nA\\begin{bmatrix}x\\\\y\\end{bmatrix}\n=\n\\begin{bmatrix}x\\\\-y\\end{bmatrix}.\n$$\n\nThe $x$-coordinate is unchanged, and the $y$-coordinate changes sign. Points\nabove the x-axis move below it, and points below move above it. This is a\nreflection over the x-axis.\n\n## What linear transformations preserve\n\nLinear transformations always preserve:\n\n- the zero vector: $T(\\mathbf{0})=\\mathbf{0}$;\n- vector addition;\n- scalar multiplication;\n- lines through the origin.\n\nThey do not necessarily preserve length, angle, or area. Some do, but many do\nnot.\n\n::: {.callout-warning}\n## Common pitfall\n\nA transformation can be linear even if it changes lengths and angles. Linearity\nis about preserving addition and scalar multiplication, not about preserving\ngeometry exactly.\n:::\n\n## Practice exercises\n\n1. What does $\\begin{bmatrix}3&0\\\\0&3\\end{bmatrix}$ do to every vector?\n2. What does $\\begin{bmatrix}1&0\\\\0&0\\end{bmatrix}$ do to $(4,5)$?\n3. What are the images of $\\mathbf{e}_1$ and $\\mathbf{e}_2$ under\n   $A=\\begin{bmatrix}2&1\\\\0&3\\end{bmatrix}$?\n4. Is $T(x,y)=(x+1,y)$ linear?\n5. Compute $\\begin{bmatrix}1&1\\\\0&1\\end{bmatrix}\\begin{bmatrix}2\\\\3\\end{bmatrix}$.\n\n::: {.callout-tip collapse=\"true\"}\n## Solutions\n\n1. It scales every vector by 3.\n2. It sends $(4,5)$ to $(4,0)$, a projection onto the x-axis.\n3. $\\mathbf{e}_1$ goes to $\\begin{bmatrix}2\\\\0\\end{bmatrix}$ and\n   $\\mathbf{e}_2$ goes to $\\begin{bmatrix}1\\\\3\\end{bmatrix}$.\n4. No. It sends $(0,0)$ to $(1,0)$, but a linear transformation must send the\n   zero vector to the zero vector.\n5. $\\begin{bmatrix}5\\\\3\\end{bmatrix}$.\n:::\n\n## Subtopic guided practice and checkpoints\n\nA matrix transformation is a rule: input vector in, output vector out. The\ncolumns show what happens to the standard basis vectors.\n\n### Lab 1: apply a transformation to one vector\n\n**Guess first.** If $T(x,y)=(2x,3y)$, what happens to the point $(1,1)$?\n\n**Guided exercise.**\n\nLet\n\n$$\nA=\\begin{bmatrix}2&0\\\\0&3\\end{bmatrix},\n\\qquad\n\\mathbf{v}=\\begin{bmatrix}1\\\\4\\end{bmatrix}.\n$$\n\nCompute\n\n$$\nA\\mathbf{v}=\\begin{bmatrix}2(1)+0(4)\\\\0(1)+3(4)\\end{bmatrix}\n=\\begin{bmatrix}2\\\\12\\end{bmatrix}.\n$$\n\nSo the vector moves from $(1,4)$ to $(2,12)$ under the transformation.\n\n::: {.practice-box}\n**Checkpoint.** Compute\n$\\begin{bmatrix}1&1\\\\0&1\\end{bmatrix}\\begin{bmatrix}3\\\\2\\end{bmatrix}$ and\ninterpret the movement.\n:::\n\n### Lab 2: read columns as images of basis vectors\n\n**Guess first.** For $A=\\begin{bmatrix}2&5\\\\1&3\\end{bmatrix}$, where does\n$\\mathbf{e}_1$ go?\n\n**Guided exercise.**\n\nThe first column tells where $\\mathbf{e}_1$ goes, and the second column tells\nwhere $\\mathbf{e}_2$ goes:\n\n$$\n\\mathbf{e}_1\\mapsto\\begin{bmatrix}2\\\\1\\end{bmatrix},\n\\qquad\n\\mathbf{e}_2\\mapsto\\begin{bmatrix}5\\\\3\\end{bmatrix}.\n$$\n\nThen any vector $\\begin{bmatrix}x\\\\y\\end{bmatrix}$ maps to\n$x$ times the first column plus $y$ times the second column.\n\n::: {.practice-box}\n**Checkpoint.** For $A=\\begin{bmatrix}0&1\\\\-1&0\\end{bmatrix}$, find the images\nof $\\mathbf{e}_1$ and $\\mathbf{e}_2$.\n:::\n\n### Lab 3: identify common transformations from their formulas\n\n**Guess first.** What does $T(x,y)=(x,0)$ do to the plane?\n\n**Guided exercise.**\n\nThe transformation $T(x,y)=(x,0)$ keeps the horizontal coordinate and removes\nthe vertical coordinate. Every point lands on the x-axis, so this is projection\nonto the x-axis.\n\nCompare common forms:\n\n```text\n(x, y) -> (2x, 2y)     scaling by 2\n(x, y) -> (x, -y)      reflection over the x-axis\n(x, y) -> (x, 0)       projection onto the x-axis\n(x, y) -> (x+y, y)     horizontal shear\n```\n\n::: {.practice-box}\n**Checkpoint.** Identify $T(x,y)=(-x,y)$ and $T(x,y)=(x+2y,y)$.\n:::\n\n### Lab 4: test whether a rule can be linear\n\n**Guess first.** Can a linear transformation send $(0,0)$ to $(1,0)$?\n\n**Guided exercise.**\n\nEvery linear transformation must satisfy $T(\\mathbf{0})=\\mathbf{0}$. The rule\n\n$$\nT(x,y)=(x+1,y)\n$$\n\nsends $(0,0)$ to $(1,0)$, so it cannot be linear. It is a translation, not a\nlinear transformation.\n\n::: {.practice-box}\n**Checkpoint.** Decide whether $T(x,y)=(3x,3y)$ can be linear, and whether\n$S(x,y)=(x,y-2)$ can be linear. Use the zero-vector test.\n:::\n\n### Lab 5: connect geometry to repeated application\n\n**Guess first.** If a matrix doubles every vector, what happens after applying\nit three times?\n\n**Guided exercise.**\n\nThe scaling matrix\n\n$$\nA=\\begin{bmatrix}2&0\\\\0&2\\end{bmatrix}\n$$\n\ndoubles every vector. Applying it three times multiplies the vector by\n$2\\cdot2\\cdot2=8$.\n\n::: {.practice-box}\n**Checkpoint.** If\n$A=\\begin{bmatrix}1&0\\\\0&-1\\end{bmatrix}$ reflects over the x-axis, what\nhappens when you apply $A$ twice?\n:::\n\n::: {.callout-tip collapse=\"true\"}\n### Lab checkpoint answers\n\n1. $\\begin{bmatrix}1&1\\\\0&1\\end{bmatrix}\\begin{bmatrix}3\\\\2\\end{bmatrix}=\n   \\begin{bmatrix}5\\\\2\\end{bmatrix}$. The shear adds the vertical coordinate to\n   the horizontal coordinate and keeps the vertical coordinate.\n2. $\\mathbf{e}_1\\mapsto\\begin{bmatrix}0\\\\-1\\end{bmatrix}$ and\n   $\\mathbf{e}_2\\mapsto\\begin{bmatrix}1\\\\0\\end{bmatrix}$.\n3. $T(x,y)=(-x,y)$ reflects over the y-axis. $T(x,y)=(x+2y,y)$ is a horizontal\n   shear.\n4. $T(x,y)=(3x,3y)$ passes the zero-vector test and can be linear. $S(x,y)=(x,y-2)$\n   fails because $S(0,0)=(0,-2)$.\n5. Applying the reflection twice returns every vector to its original position.\n:::\n\n## Transformation guessing game\n\nGuess the geometric action from a matrix or a property.\n\n<script type=\"application/json\" class=\"edu-math-linear-algebra-checkpoint-config\">\n{\n  \"topic\": \"linear-transformations\",\n  \"title\": \"Linear transformations checkpoint\",\n  \"defaultTotal\": 4\n}\n</script>\n\n<div class=\"quiz-shell edu-math-linear-algebra-checkpoint\" data-default-total=\"4\"></div>\n\n<script>\n  (() => {\n    const DEFAULT_TOPIC = \"vectors\";\n    const BANKS = {\n      vectors: [normQuestion, dotProductQuestion, perpendicularQuestion],\n      matrices: [shapeQuestion, identityQuestion, entryQuestion],\n      \"matrix-operations\": [productDefinedQuestion, matrixVectorQuestion, scalarQuestion],\n      \"systems-and-elimination\": [systemClassQuestion, eliminationMoveQuestion, solutionCheckQuestion],\n      \"linear-transformations\": [transformationQuestion, basisQuestion, preserveQuestion],\n      \"eigenvalues-eigenvectors\": [eigenvalueQuestion, eigenvectorQuestion, repeatedQuestion],\n      \"linear-algebra-cumulative-review\": [normQuestion, shapeQuestion, productDefinedQuestion, systemClassQuestion, transformationQuestion, eigenvalueQuestion],\n    };\n\n    document.querySelectorAll(\".edu-math-linear-algebra-checkpoint:not([data-ready])\").forEach(init);\n\n    function init(root) {\n      const config = readConfig(root);\n      const state = { config, total: config.defaultTotal, index: 0, score: 0 };\n      root.dataset.ready = \"true\";\n      renderStart(root, state);\n    }\n\n    function readConfig(root) {\n      const node = root.previousElementSibling;\n      let parsed = {};\n      if (node?.classList?.contains(\"edu-math-linear-algebra-checkpoint-config\")) {\n        try {\n          parsed = JSON.parse(node.textContent || \"{}\");\n        } catch {\n          parsed = {};\n        }\n      }\n      return {\n        topic: parsed.topic || root.dataset.topic || DEFAULT_TOPIC,\n        title: parsed.title || \"Linear algebra checkpoint\",\n        defaultTotal: Math.max(1, Math.min(20, Number(parsed.defaultTotal || 4))),\n      };\n    }\n\n    function renderStart(root, state) {\n      root.replaceChildren();\n      const input = element(\"input\", {\n        class: \"quiz-count\",\n        type: \"number\",\n        min: \"1\",\n        max: \"20\",\n        value: String(state.total),\n      });\n      const button = element(\"button\", { type: \"button\" }, \"Start checkpoint\");\n      button.addEventListener(\"click\", () => {\n        state.total = Math.max(1, Math.min(20, Number(input.value) || 4));\n        state.index = 0;\n        state.score = 0;\n        renderQuestion(root, state, makeQuestion(state.config.topic));\n      });\n      root.append(\n        element(\"p\", { class: \"quiz-topic\" }, state.config.title),\n        element(\"div\", { class: \"quiz-start\" }, element(\"label\", {}, element(\"span\", {}, \"Number of questions\"), input), button),\n      );\n    }\n\n    function renderQuestion(root, state, question) {\n      root.replaceChildren();\n      let answered = false;\n      const progress = element(\n        \"div\",\n        { class: \"quiz-progress\" },\n        element(\"span\", {}, `Question ${state.index + 1} of ${state.total}`),\n        element(\"span\", {}, `Score: ${state.score}/${state.total}`),\n      );\n      const feedback = element(\"p\", { class: \"quiz-feedback\", \"aria-live\": \"polite\" });\n      const next = element(\"button\", { type: \"button\", disabled: \"\" }, \"Next question\");\n      const options = radioList(question.options, (selected) => {\n        if (answered) return;\n        answered = true;\n        const correct = selected === question.answer;\n        if (correct) state.score += 1;\n        feedback.className = correct ? \"quiz-feedback correct\" : \"quiz-feedback wrong\";\n        feedback.replaceChildren(\n          correct ? \"Correct.\" : `Not quite. The answer is ${question.answer}.`,\n          \" \",\n          question.explanation,\n        );\n        options.querySelectorAll(\"input\").forEach((input) => {\n          input.disabled = true;\n        });\n        progress.lastElementChild.textContent = `Score: ${state.score}/${state.total}`;\n        next.disabled = false;\n      });\n      next.addEventListener(\"click\", () => {\n        state.index += 1;\n        if (state.index >= state.total) renderDone(root, state);\n        else renderQuestion(root, state, makeQuestion(state.config.topic));\n      });\n      root.append(progress, stimulus(question.stimulus), element(\"p\", { class: \"quiz-prompt\" }, question.prompt), feedback, options, next);\n    }\n\n    function renderDone(root, state) {\n      root.replaceChildren();\n      const restart = element(\"button\", { type: \"button\" }, \"Try again\");\n      restart.addEventListener(\"click\", () => renderStart(root, state));\n      root.append(element(\"h3\", {}, \"Checkpoint complete\"), element(\"p\", {}, `Final score: ${state.score}/${state.total}`), restart);\n    }\n\n    function makeQuestion(topic) {\n      const bank = BANKS[topic] || BANKS[DEFAULT_TOPIC];\n      return choice(bank)();\n    }\n\n    function normQuestion() {\n      const pairs = [[3, 4, 5], [5, 12, 13], [6, 8, 10]];\n      const [a, b, n] = choice(pairs);\n      return question({\n        prompt: \"Guess the vector length.\",\n        stimulus: `v = (${a}, ${b})`,\n        answer: String(n),\n        distractors: [String(a + b), String(Math.abs(a - b)), String(a * b)],\n        explanation: \"Use the Pythagorean formula: sqrt(a²+b²).\",\n      });\n    }\n\n    function dotProductQuestion() {\n      return question({\n        prompt: \"Compute the dot product.\",\n        stimulus: \"(1, 2) · (3, 4)\",\n        answer: \"11\",\n        distractors: [\"10\", \"14\", \"24\"],\n        explanation: \"Multiply matching coordinates and add: 1·3 + 2·4 = 11.\",\n      });\n    }\n\n    function perpendicularQuestion() {\n      return question({\n        prompt: \"Which dot product shows nonzero vectors are perpendicular?\",\n        stimulus: \"orthogonality test\",\n        answer: \"0\",\n        distractors: [\"1\", \"-1\", \"the vector length\"],\n        explanation: \"A dot product of zero means the vectors meet at a right angle.\",\n      });\n    }\n\n    function shapeQuestion() {\n      return question({\n        prompt: \"What is the shape of a matrix with 2 rows and 3 columns?\",\n        stimulus: \"rows by columns\",\n        answer: \"2 x 3\",\n        distractors: [\"3 x 2\", \"5\", \"6 x 1\"],\n        explanation: \"Shape is always written rows first, columns second.\",\n      });\n    }\n\n    function identityQuestion() {\n      return question({\n        prompt: \"What does the identity matrix do to a vector?\",\n        stimulus: \"I v\",\n        answer: \"leaves it unchanged\",\n        distractors: [\"turns it into zero\", \"doubles it\", \"swaps coordinates\"],\n        explanation: \"The identity matrix is the do-nothing matrix for multiplication.\",\n      });\n    }\n\n    function entryQuestion() {\n      return question({\n        prompt: \"Which entry is in row 2, column 1?\",\n        stimulus: \"[[4, 5], [7, 8]]\",\n        answer: \"7\",\n        distractors: [\"4\", \"5\", \"8\"],\n        explanation: \"Move to the second row, then the first column.\",\n      });\n    }\n\n    function productDefinedQuestion() {\n      return question({\n        prompt: \"Is this matrix product defined?\",\n        stimulus: \"(2 x 3)(3 x 4)\",\n        answer: \"yes\",\n        distractors: [\"no\", \"only if square\", \"only with a vector\"],\n        explanation: \"The inner dimensions match: 3 and 3.\",\n      });\n    }\n\n    function matrixVectorQuestion() {\n      return question({\n        prompt: \"Find the first entry of A v.\",\n        stimulus: \"A=[[2,1],[0,3]], v=(4,5)\",\n        answer: \"13\",\n        distractors: [\"8\", \"15\", \"17\"],\n        explanation: \"The first row dot v is 2·4 + 1·5 = 13.\",\n      });\n    }\n\n    function scalarQuestion() {\n      return question({\n        prompt: \"What is 3 times the matrix entry -2?\",\n        stimulus: \"scalar multiplication\",\n        answer: \"-6\",\n        distractors: [\"6\", \"-2\", \"1\"],\n        explanation: \"Scalar multiplication multiplies every entry by the scalar.\",\n      });\n    }\n\n    function systemClassQuestion() {\n      return question({\n        prompt: \"Classify the system.\",\n        stimulus: \"x+y=2 and 2x+2y=4\",\n        answer: \"infinitely many solutions\",\n        distractors: [\"one solution\", \"no solution\", \"not linear\"],\n        explanation: \"The second equation is a multiple of the first, so the lines coincide.\",\n      });\n    }\n\n    function eliminationMoveQuestion() {\n      return question({\n        prompt: \"Which move eliminates x from the second equation?\",\n        stimulus: \"E1: x+y=5, E2: x-y=1\",\n        answer: \"E2 - E1\",\n        distractors: [\"E2 + E1\", \"2E1\", \"swap columns\"],\n        explanation: \"Subtracting gives -2y = -4, with no x term.\",\n      });\n    }\n\n    function solutionCheckQuestion() {\n      return question({\n        prompt: \"Does (3,2) solve x+y=5 and x-y=1?\",\n        stimulus: \"substitute both equations\",\n        answer: \"yes\",\n        distractors: [\"no\", \"only the first\", \"only the second\"],\n        explanation: \"3+2=5 and 3-2=1.\",\n      });\n    }\n\n    function transformationQuestion() {\n      return question({\n        prompt: \"What does [[1,0],[0,0]] do?\",\n        stimulus: \"T(x,y)=(x,0)\",\n        answer: \"projects onto the x-axis\",\n        distractors: [\"rotates 90 degrees\", \"reflects over y-axis\", \"doubles every vector\"],\n        explanation: \"It keeps x and replaces y by 0.\",\n      });\n    }\n\n    function basisQuestion() {\n      return question({\n        prompt: \"Why do basis vectors matter?\",\n        stimulus: \"2D linear transformations\",\n        answer: \"their images determine the whole transformation\",\n        distractors: [\"they are always eigenvectors\", \"they remove multiplication\", \"they make all matrices diagonal\"],\n        explanation: \"Every vector is a combination of basis vectors, and linear maps preserve combinations.\",\n      });\n    }\n\n    function preserveQuestion() {\n      return question({\n        prompt: \"Which property must a linear transformation preserve?\",\n        stimulus: \"linearity\",\n        answer: \"addition and scalar multiplication\",\n        distractors: [\"distance always\", \"area always\", \"all angles\"],\n        explanation: \"Linear means T(u+v)=T(u)+T(v) and T(cv)=cT(v).\",\n      });\n    }\n\n    function eigenvalueQuestion() {\n      return question({\n        prompt: \"If A v = 4v, what is the eigenvalue?\",\n        stimulus: \"v is nonzero\",\n        answer: \"4\",\n        distractors: [\"v\", \"0\", \"A\"],\n        explanation: \"The scalar multiplying v is the eigenvalue.\",\n      });\n    }\n\n    function eigenvectorQuestion() {\n      return question({\n        prompt: \"Is the zero vector an eigenvector?\",\n        stimulus: \"definition check\",\n        answer: \"no\",\n        distractors: [\"yes\", \"only for diagonal matrices\", \"only if lambda is 0\"],\n        explanation: \"Eigenvectors must be nonzero so that they represent a direction.\",\n      });\n    }\n\n    function repeatedQuestion() {\n      return question({\n        prompt: \"Why are eigenvectors useful for repeated multiplication?\",\n        stimulus: \"A, A², A³, ...\",\n        answer: \"they keep the same direction while scaling\",\n        distractors: [\"they make all entries zero\", \"they avoid addition\", \"they always have length 1\"],\n        explanation: \"Repeated applications multiply an eigenvector by powers of its eigenvalue.\",\n      });\n    }\n\n    function question({ prompt, stimulus, answer, distractors, explanation }) {\n      return { prompt, stimulus, answer, options: shuffle([answer, ...distractors]), explanation };\n    }\n\n    function radioList(options, onSelect) {\n      const name = `linear-algebra-checkpoint-${Math.random().toString(36).slice(2)}`;\n      const container = element(\"div\", { class: \"quiz-options\" });\n      options.forEach((option) => {\n        const input = element(\"input\", { type: \"radio\", name, value: option });\n        input.addEventListener(\"change\", () => onSelect(option));\n        container.append(element(\"label\", {}, input, element(\"span\", {}, option)));\n      });\n      return container;\n    }\n\n    function stimulus(text) {\n      return element(\"pre\", { class: \"quiz-stimulus\" }, text);\n    }\n\n    function element(tag, attrs = {}, ...children) {\n      const node = document.createElement(tag);\n      Object.entries(attrs).forEach(([key, value]) => node.setAttribute(key, value));\n      node.append(...children);\n      return node;\n    }\n\n    function choice(values) {\n      return values[Math.floor(Math.random() * values.length)];\n    }\n\n    function shuffle(values) {\n      const copy = values.slice();\n      for (let index = copy.length - 1; index > 0; index -= 1) {\n        const other = Math.floor(Math.random() * (index + 1));\n        [copy[index], copy[other]] = [copy[other], copy[index]];\n      }\n      return copy;\n    }\n  })();\n</script>\n\n\n## Using this lesson with edumath and SymPy\n\nThe edumath plotting helpers create Matplotlib scenes that can be rendered in\nPython environments. SymPy can verify the algebra exactly.\n\n::: {#1f09d7d3 .cell execution_count=1}\n``` {.python .cell-code}\nfrom edumath.linear_algebra import matrix_vector_product\n\nA = [[1, 1], [0, 1]]\nv = [2, 3]\nprint(matrix_vector_product(A, v))\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n[5. 3.]\n```\n:::\n:::\n\n\n::: {#65ffe9ae .cell execution_count=2}\n``` {.python .cell-code}\nimport sympy as sp\n\nx, y = sp.symbols(\"x y\")\nA = sp.Matrix([[1, 1], [0, 1]])\nvector = sp.Matrix([x, y])\nA * vector\n```\n\n::: {.cell-output .cell-output-display .cell-output-markdown execution_count=2}\n$\\displaystyle \\left[\\begin{matrix}x + y\\\\y\\end{matrix}\\right]$\n:::\n:::\n\n\n::: {#d183962c .cell execution_count=3}\n``` {.python .cell-code}\nfrom edumath.linear_algebra import basis_transformation_scene\n\nscene = basis_transformation_scene([[1, 1], [0, 1]])\n# In a Python display environment, use: scene.render()\nscene.title\n```\n\n::: {.cell-output .cell-output-display execution_count=3}\n```\n'Matrix transformation'\n```\n:::\n:::\n\n\n",
    "supporting": ["linear-transformations_files/figure-html"],
    "filters": [],
    "includes": {}
  }
}
