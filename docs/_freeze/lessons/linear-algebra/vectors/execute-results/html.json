{
  "hash": "3f04f6c99ab20ed8e0511223cf69deca",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: Vectors\ndescription: Vectors as quantities with magnitude, direction, and coordinates.\nexecute:\n  echo: false\n---\n\nA **vector** is an ordered list of numbers. In two dimensions, the vector\n$\\langle 3,4\\rangle$ can mean \"move 3 units right and 4 units up.\" The same\nvector can also represent a point, a velocity, a force, a row of data, or a pair\nof unknowns. Linear algebra starts with vectors because they let us talk about\nmany related numbers as one object.\n\n## What you should be able to do\n\nBy the end of this lesson, you should be able to:\n\n- read vectors as coordinate lists and as arrows in the plane;\n- add vectors and multiply vectors by scalars;\n- compute vector length using the distance formula;\n- compute a dot product;\n- explain why a zero dot product means perpendicular vectors;\n- verify vector calculations with edumath, NumPy, and SymPy.\n\n## Coordinates, arrows, and movement\n\nA vector in two dimensions is often written\n\n$$\n\\mathbf{v}=\\begin{bmatrix}a\\\\b\\end{bmatrix}\n\\quad \\text{or} \\quad\n\\langle a,b\\rangle.\n$$\n\nThe first coordinate tells horizontal movement. The second coordinate tells\nvertical movement. The vector $\\langle 3,4\\rangle$ can be drawn as an arrow from\n$(0,0)$ to $(3,4)$.\n\n::: {.callout-note}\n## Coordinate reminder\n\nThe ordered pair $(3,4)$ names a point. The vector $\\langle 3,4\\rangle$ names a\nmovement or displacement. They are drawn in similar places, but the vector idea\nemphasizes direction and length.\n:::\n\n## Vector addition\n\nTo add vectors, add matching coordinates:\n\n$$\n\\begin{bmatrix}a\\\\b\\end{bmatrix}\n+\n\\begin{bmatrix}c\\\\d\\end{bmatrix}\n=\n\\begin{bmatrix}a+c\\\\b+d\\end{bmatrix}.\n$$\n\nGeometrically, vector addition means doing one movement and then the other.\nFor example,\n\n$$\n\\begin{bmatrix}3\\\\1\\end{bmatrix}\n+\n\\begin{bmatrix}-2\\\\4\\end{bmatrix}\n=\n\\begin{bmatrix}1\\\\5\\end{bmatrix}.\n$$\n\nYou moved 3 right and 1 up, then 2 left and 4 up. The total movement is 1 right\nand 5 up.\n\n## Scalar multiplication\n\nA **scalar** is a single number. Scalar multiplication stretches, shrinks, or\nflips a vector:\n\n$$\n3\\begin{bmatrix}2\\\\-1\\end{bmatrix}\n=\n\\begin{bmatrix}6\\\\-3\\end{bmatrix}.\n$$\n\nIf the scalar is negative, the vector reverses direction. If the scalar is\nbetween 0 and 1, the vector gets shorter.\n\n## Length of a vector\n\nThe length of a vector is also called its **norm** or **magnitude**. In two\ndimensions,\n\n$$\n\\lVert \\mathbf{v} \\rVert\n= \\sqrt{a^2+b^2}\n\\quad \\text{for} \\quad\n\\mathbf{v}=\\langle a,b\\rangle.\n$$\n\nThis is the Pythagorean theorem. The vector $\\langle 3,4\\rangle$ makes a right\ntriangle with legs 3 and 4, so its length is\n\n$$\n\\sqrt{3^2+4^2}=\\sqrt{9+16}=\\sqrt{25}=5.\n$$\n\n## Dot products\n\nThe **dot product** combines two vectors and returns one number:\n\n$$\n\\langle a,b\\rangle \\cdot \\langle c,d\\rangle = ac+bd.\n$$\n\nExample:\n\n$$\n\\langle 1,2\\rangle \\cdot \\langle 3,4\\rangle\n= 1\\cdot 3 + 2\\cdot 4\n= 11.\n$$\n\nThe dot product is not just arithmetic. It measures how much two vectors point\nin the same direction.\n\n- A positive dot product means the vectors point generally the same way.\n- A negative dot product means they point generally opposite ways.\n- A zero dot product, for nonzero vectors, means they are perpendicular.\n\n::: {.callout-warning}\n## Common pitfall\n\nA dot product produces a **scalar**, not a vector. Do not write\n$\\langle 1,2\\rangle \\cdot \\langle 3,4\\rangle = \\langle 3,8\\rangle$. Multiplying\nmatching coordinates is only the first step; you must add the products.\n:::\n\n## Worked example: compare two vectors\n\nLet $\\mathbf{u}=\\langle 2,1\\rangle$ and $\\mathbf{v}=\\langle -1,2\\rangle$.\n\n1. Add them:\n   $$\n   \\mathbf{u}+\\mathbf{v}=\\langle 2+(-1),1+2\\rangle=\\langle 1,3\\rangle.\n   $$\n2. Find the length of $\\mathbf{u}$:\n   $$\n   \\lVert \\mathbf{u}\\rVert=\\sqrt{2^2+1^2}=\\sqrt{5}.\n   $$\n3. Compute the dot product:\n   $$\n   \\mathbf{u}\\cdot\\mathbf{v}=2(-1)+1(2)=0.\n   $$\n4. Interpret: because the dot product is zero and neither vector is the zero\n   vector, the vectors are perpendicular.\n\n## Practice exercises\n\nTry these before opening the solutions.\n\n1. Compute $\\lVert \\langle 6,8\\rangle \\rVert$.\n2. Compute $\\langle 1,0\\rangle \\cdot \\langle 0,1\\rangle$.\n3. Add $\\langle -2,5\\rangle + \\langle 4,-1\\rangle$.\n4. Multiply $-2\\langle 3,-4\\rangle$.\n5. Are $\\langle 3,1\\rangle$ and $\\langle -1,3\\rangle$ perpendicular?\n\n::: {.callout-tip collapse=\"true\"}\n## Solutions\n\n1. $\\sqrt{6^2+8^2}=\\sqrt{100}=10$.\n2. $1\\cdot 0+0\\cdot 1=0$.\n3. $\\langle 2,4\\rangle$.\n4. $\\langle -6,8\\rangle$.\n5. Yes. The dot product is $3(-1)+1(3)=0$.\n:::\n\n## Subtopic guided practice and checkpoints\n\nVectors become easier when you separate three ideas: coordinates tell movement,\nlength measures size, and dot products compare directions.\n\n### Lab 1: read a vector as movement\n\n**Guess first.** Does the vector `<-2, 5>` move right or left? Up or down?\n\n**Guided exercise.**\n\nRead the coordinates in order:\n\n1. The first coordinate is `-2`, so the horizontal movement is 2 units left.\n2. The second coordinate is `5`, so the vertical movement is 5 units up.\n3. The vector can be drawn from any starting point, but its movement is always\n   “left 2, up 5.”\n\n::: {.practice-box}\n**Checkpoint.** Describe the movement of `<4, -3>` in words. Then say where\nthe arrow ends if it starts at `(0,0)`.\n:::\n\n### Lab 2: add vectors coordinate by coordinate\n\n**Guess first.** If one vector moves right and another moves left, can their\nhorizontal parts cancel?\n\n**Guided exercise.**\n\nAdd `<3, -1> + <-5, 4>` by matching positions:\n\n```text\nhorizontal:  3 + (-5) = -2\nvertical:   -1 + 4    = 3\n```\n\nSo\n\n```text\n<3, -1> + <-5, 4> = <-2, 3>\n```\n\nThe result means the combined movement is 2 units left and 3 units up.\n\n::: {.practice-box}\n**Checkpoint.** Compute `<6, 2> + <-1, -7>` and explain which coordinate changed\ndirection.\n:::\n\n### Lab 3: scale a vector without mixing coordinates\n\n**Guess first.** What does a negative scalar do to the direction of a vector?\n\n**Guided exercise.**\n\nCompute `-3<2, -4>` by multiplying every coordinate:\n\n```text\n-3<2, -4> = <-6, 12>\n```\n\nBecause the scalar is negative, the vector points in the opposite direction from\nthe original vector.\n\n::: {.practice-box}\n**Checkpoint.** Compute `(1/2)<8, -6>` and describe how the length changes.\n:::\n\n### Lab 4: compute length with the Pythagorean theorem\n\n**Guess first.** Is the length of `<5, 12>` closer to 12, 13, or 17?\n\n**Guided exercise.**\n\nUse the norm formula:\n\n```text\nlength of <5, 12> = sqrt(5^2 + 12^2)\n                  = sqrt(25 + 144)\n                  = sqrt(169)\n                  = 13\n```\n\nLength is never negative. It measures distance from the tail of the arrow to the\ntip.\n\n::: {.practice-box}\n**Checkpoint.** Find the length of `<-8, 15>`.\n:::\n\n### Lab 5: use the dot product to compare directions\n\n**Guess first.** If the dot product of two nonzero vectors is `0`, are the\nvectors pointing the same way, opposite ways, or perpendicular?\n\n**Guided exercise.**\n\nLet `u = <4, 1>` and `v = <-1, 4>`. Then\n\n```text\nu dot v = 4(-1) + 1(4) = 0\n```\n\nThe zero dot product tells us these nonzero vectors are perpendicular.\n\n::: {.practice-box}\n**Checkpoint.** Compute `<2, 3> dot <5, -1>`. Are the vectors perpendicular?\n:::\n\n::: {.callout-tip collapse=\"true\"}\n### Lab checkpoint answers\n\n1. `<4, -3>` moves right 4 and down 3. Starting at `(0,0)`, it ends at\n   `(4,-3)`.\n2. `<6, 2> + <-1, -7> = <5, -5>`. The vertical coordinate changed from positive\n   to negative.\n3. `(1/2)<8, -6> = <4, -3>`. The vector points the same direction and has half\n   the length.\n4. `sqrt((-8)^2 + 15^2) = sqrt(289) = 17`.\n5. The dot product is `2(5) + 3(-1) = 7`, so the vectors are not perpendicular.\n:::\n\n## Vector guessing game\n\nGuess lengths, dot products, and perpendicular relationships. If you miss a\nquestion, read the feedback and connect it to the formula.\n\n<script type=\"application/json\" class=\"edu-math-linear-algebra-checkpoint-config\">\n{\n  \"topic\": \"vectors\",\n  \"title\": \"Vectors checkpoint\",\n  \"defaultTotal\": 4\n}\n</script>\n\n<div class=\"quiz-shell edu-math-linear-algebra-checkpoint\" data-default-total=\"4\"></div>\n\n<script>\n  (() => {\n    const DEFAULT_TOPIC = \"vectors\";\n    const BANKS = {\n      vectors: [normQuestion, dotProductQuestion, perpendicularQuestion],\n      matrices: [shapeQuestion, identityQuestion, entryQuestion],\n      \"matrix-operations\": [productDefinedQuestion, matrixVectorQuestion, scalarQuestion],\n      \"systems-and-elimination\": [systemClassQuestion, eliminationMoveQuestion, solutionCheckQuestion],\n      \"linear-transformations\": [transformationQuestion, basisQuestion, preserveQuestion],\n      \"eigenvalues-eigenvectors\": [eigenvalueQuestion, eigenvectorQuestion, repeatedQuestion],\n      \"linear-algebra-cumulative-review\": [normQuestion, shapeQuestion, productDefinedQuestion, systemClassQuestion, transformationQuestion, eigenvalueQuestion],\n    };\n\n    document.querySelectorAll(\".edu-math-linear-algebra-checkpoint:not([data-ready])\").forEach(init);\n\n    function init(root) {\n      const config = readConfig(root);\n      const state = { config, total: config.defaultTotal, index: 0, score: 0 };\n      root.dataset.ready = \"true\";\n      renderStart(root, state);\n    }\n\n    function readConfig(root) {\n      const node = root.previousElementSibling;\n      let parsed = {};\n      if (node?.classList?.contains(\"edu-math-linear-algebra-checkpoint-config\")) {\n        try {\n          parsed = JSON.parse(node.textContent || \"{}\");\n        } catch {\n          parsed = {};\n        }\n      }\n      return {\n        topic: parsed.topic || root.dataset.topic || DEFAULT_TOPIC,\n        title: parsed.title || \"Linear algebra checkpoint\",\n        defaultTotal: Math.max(1, Math.min(20, Number(parsed.defaultTotal || 4))),\n      };\n    }\n\n    function renderStart(root, state) {\n      root.replaceChildren();\n      const input = element(\"input\", {\n        class: \"quiz-count\",\n        type: \"number\",\n        min: \"1\",\n        max: \"20\",\n        value: String(state.total),\n      });\n      const button = element(\"button\", { type: \"button\" }, \"Start checkpoint\");\n      button.addEventListener(\"click\", () => {\n        state.total = Math.max(1, Math.min(20, Number(input.value) || 4));\n        state.index = 0;\n        state.score = 0;\n        renderQuestion(root, state, makeQuestion(state.config.topic));\n      });\n      root.append(\n        element(\"p\", { class: \"quiz-topic\" }, state.config.title),\n        element(\"div\", { class: \"quiz-start\" }, element(\"label\", {}, element(\"span\", {}, \"Number of questions\"), input), button),\n      );\n    }\n\n    function renderQuestion(root, state, question) {\n      root.replaceChildren();\n      let answered = false;\n      const progress = element(\n        \"div\",\n        { class: \"quiz-progress\" },\n        element(\"span\", {}, `Question ${state.index + 1} of ${state.total}`),\n        element(\"span\", {}, `Score: ${state.score}/${state.total}`),\n      );\n      const feedback = element(\"p\", { class: \"quiz-feedback\", \"aria-live\": \"polite\" });\n      const next = element(\"button\", { type: \"button\", disabled: \"\" }, \"Next question\");\n      const options = radioList(question.options, (selected) => {\n        if (answered) return;\n        answered = true;\n        const correct = selected === question.answer;\n        if (correct) state.score += 1;\n        feedback.className = correct ? \"quiz-feedback correct\" : \"quiz-feedback wrong\";\n        feedback.replaceChildren(\n          correct ? \"Correct.\" : `Not quite. The answer is ${question.answer}.`,\n          \" \",\n          question.explanation,\n        );\n        options.querySelectorAll(\"input\").forEach((input) => {\n          input.disabled = true;\n        });\n        progress.lastElementChild.textContent = `Score: ${state.score}/${state.total}`;\n        next.disabled = false;\n      });\n      next.addEventListener(\"click\", () => {\n        state.index += 1;\n        if (state.index >= state.total) renderDone(root, state);\n        else renderQuestion(root, state, makeQuestion(state.config.topic));\n      });\n      root.append(progress, stimulus(question.stimulus), element(\"p\", { class: \"quiz-prompt\" }, question.prompt), feedback, options, next);\n    }\n\n    function renderDone(root, state) {\n      root.replaceChildren();\n      const restart = element(\"button\", { type: \"button\" }, \"Try again\");\n      restart.addEventListener(\"click\", () => renderStart(root, state));\n      root.append(element(\"h3\", {}, \"Checkpoint complete\"), element(\"p\", {}, `Final score: ${state.score}/${state.total}`), restart);\n    }\n\n    function makeQuestion(topic) {\n      const bank = BANKS[topic] || BANKS[DEFAULT_TOPIC];\n      return choice(bank)();\n    }\n\n    function normQuestion() {\n      const pairs = [[3, 4, 5], [5, 12, 13], [6, 8, 10]];\n      const [a, b, n] = choice(pairs);\n      return question({\n        prompt: \"Guess the vector length.\",\n        stimulus: `v = (${a}, ${b})`,\n        answer: String(n),\n        distractors: [String(a + b), String(Math.abs(a - b)), String(a * b)],\n        explanation: \"Use the Pythagorean formula: sqrt(a²+b²).\",\n      });\n    }\n\n    function dotProductQuestion() {\n      return question({\n        prompt: \"Compute the dot product.\",\n        stimulus: \"(1, 2) · (3, 4)\",\n        answer: \"11\",\n        distractors: [\"10\", \"14\", \"24\"],\n        explanation: \"Multiply matching coordinates and add: 1·3 + 2·4 = 11.\",\n      });\n    }\n\n    function perpendicularQuestion() {\n      return question({\n        prompt: \"Which dot product shows nonzero vectors are perpendicular?\",\n        stimulus: \"orthogonality test\",\n        answer: \"0\",\n        distractors: [\"1\", \"-1\", \"the vector length\"],\n        explanation: \"A dot product of zero means the vectors meet at a right angle.\",\n      });\n    }\n\n    function shapeQuestion() {\n      return question({\n        prompt: \"What is the shape of a matrix with 2 rows and 3 columns?\",\n        stimulus: \"rows by columns\",\n        answer: \"2 x 3\",\n        distractors: [\"3 x 2\", \"5\", \"6 x 1\"],\n        explanation: \"Shape is always written rows first, columns second.\",\n      });\n    }\n\n    function identityQuestion() {\n      return question({\n        prompt: \"What does the identity matrix do to a vector?\",\n        stimulus: \"I v\",\n        answer: \"leaves it unchanged\",\n        distractors: [\"turns it into zero\", \"doubles it\", \"swaps coordinates\"],\n        explanation: \"The identity matrix is the do-nothing matrix for multiplication.\",\n      });\n    }\n\n    function entryQuestion() {\n      return question({\n        prompt: \"Which entry is in row 2, column 1?\",\n        stimulus: \"[[4, 5], [7, 8]]\",\n        answer: \"7\",\n        distractors: [\"4\", \"5\", \"8\"],\n        explanation: \"Move to the second row, then the first column.\",\n      });\n    }\n\n    function productDefinedQuestion() {\n      return question({\n        prompt: \"Is this matrix product defined?\",\n        stimulus: \"(2 x 3)(3 x 4)\",\n        answer: \"yes\",\n        distractors: [\"no\", \"only if square\", \"only with a vector\"],\n        explanation: \"The inner dimensions match: 3 and 3.\",\n      });\n    }\n\n    function matrixVectorQuestion() {\n      return question({\n        prompt: \"Find the first entry of A v.\",\n        stimulus: \"A=[[2,1],[0,3]], v=(4,5)\",\n        answer: \"13\",\n        distractors: [\"8\", \"15\", \"17\"],\n        explanation: \"The first row dot v is 2·4 + 1·5 = 13.\",\n      });\n    }\n\n    function scalarQuestion() {\n      return question({\n        prompt: \"What is 3 times the matrix entry -2?\",\n        stimulus: \"scalar multiplication\",\n        answer: \"-6\",\n        distractors: [\"6\", \"-2\", \"1\"],\n        explanation: \"Scalar multiplication multiplies every entry by the scalar.\",\n      });\n    }\n\n    function systemClassQuestion() {\n      return question({\n        prompt: \"Classify the system.\",\n        stimulus: \"x+y=2 and 2x+2y=4\",\n        answer: \"infinitely many solutions\",\n        distractors: [\"one solution\", \"no solution\", \"not linear\"],\n        explanation: \"The second equation is a multiple of the first, so the lines coincide.\",\n      });\n    }\n\n    function eliminationMoveQuestion() {\n      return question({\n        prompt: \"Which move eliminates x from the second equation?\",\n        stimulus: \"E1: x+y=5, E2: x-y=1\",\n        answer: \"E2 - E1\",\n        distractors: [\"E2 + E1\", \"2E1\", \"swap columns\"],\n        explanation: \"Subtracting gives -2y = -4, with no x term.\",\n      });\n    }\n\n    function solutionCheckQuestion() {\n      return question({\n        prompt: \"Does (3,2) solve x+y=5 and x-y=1?\",\n        stimulus: \"substitute both equations\",\n        answer: \"yes\",\n        distractors: [\"no\", \"only the first\", \"only the second\"],\n        explanation: \"3+2=5 and 3-2=1.\",\n      });\n    }\n\n    function transformationQuestion() {\n      return question({\n        prompt: \"What does [[1,0],[0,0]] do?\",\n        stimulus: \"T(x,y)=(x,0)\",\n        answer: \"projects onto the x-axis\",\n        distractors: [\"rotates 90 degrees\", \"reflects over y-axis\", \"doubles every vector\"],\n        explanation: \"It keeps x and replaces y by 0.\",\n      });\n    }\n\n    function basisQuestion() {\n      return question({\n        prompt: \"Why do basis vectors matter?\",\n        stimulus: \"2D linear transformations\",\n        answer: \"their images determine the whole transformation\",\n        distractors: [\"they are always eigenvectors\", \"they remove multiplication\", \"they make all matrices diagonal\"],\n        explanation: \"Every vector is a combination of basis vectors, and linear maps preserve combinations.\",\n      });\n    }\n\n    function preserveQuestion() {\n      return question({\n        prompt: \"Which property must a linear transformation preserve?\",\n        stimulus: \"linearity\",\n        answer: \"addition and scalar multiplication\",\n        distractors: [\"distance always\", \"area always\", \"all angles\"],\n        explanation: \"Linear means T(u+v)=T(u)+T(v) and T(cv)=cT(v).\",\n      });\n    }\n\n    function eigenvalueQuestion() {\n      return question({\n        prompt: \"If A v = 4v, what is the eigenvalue?\",\n        stimulus: \"v is nonzero\",\n        answer: \"4\",\n        distractors: [\"v\", \"0\", \"A\"],\n        explanation: \"The scalar multiplying v is the eigenvalue.\",\n      });\n    }\n\n    function eigenvectorQuestion() {\n      return question({\n        prompt: \"Is the zero vector an eigenvector?\",\n        stimulus: \"definition check\",\n        answer: \"no\",\n        distractors: [\"yes\", \"only for diagonal matrices\", \"only if lambda is 0\"],\n        explanation: \"Eigenvectors must be nonzero so that they represent a direction.\",\n      });\n    }\n\n    function repeatedQuestion() {\n      return question({\n        prompt: \"Why are eigenvectors useful for repeated multiplication?\",\n        stimulus: \"A, A², A³, ...\",\n        answer: \"they keep the same direction while scaling\",\n        distractors: [\"they make all entries zero\", \"they avoid addition\", \"they always have length 1\"],\n        explanation: \"Repeated applications multiply an eigenvector by powers of its eigenvalue.\",\n      });\n    }\n\n    function question({ prompt, stimulus, answer, distractors, explanation }) {\n      return { prompt, stimulus, answer, options: shuffle([answer, ...distractors]), explanation };\n    }\n\n    function radioList(options, onSelect) {\n      const name = `linear-algebra-checkpoint-${Math.random().toString(36).slice(2)}`;\n      const container = element(\"div\", { class: \"quiz-options\" });\n      options.forEach((option) => {\n        const input = element(\"input\", { type: \"radio\", name, value: option });\n        input.addEventListener(\"change\", () => onSelect(option));\n        container.append(element(\"label\", {}, input, element(\"span\", {}, option)));\n      });\n      return container;\n    }\n\n    function stimulus(text) {\n      return element(\"pre\", { class: \"quiz-stimulus\" }, text);\n    }\n\n    function element(tag, attrs = {}, ...children) {\n      const node = document.createElement(tag);\n      Object.entries(attrs).forEach(([key, value]) => node.setAttribute(key, value));\n      node.append(...children);\n      return node;\n    }\n\n    function choice(values) {\n      return values[Math.floor(Math.random() * values.length)];\n    }\n\n    function shuffle(values) {\n      const copy = values.slice();\n      for (let index = copy.length - 1; index > 0; index -= 1) {\n        const other = Math.floor(Math.random() * (index + 1));\n        [copy[index], copy[other]] = [copy[other], copy[index]];\n      }\n      return copy;\n    }\n  })();\n</script>\n\n\n## Using this lesson with edumath and SymPy\n\nUse the visible code below to reproduce the main computations. In a browser,\nthese examples can also be adapted for PyScript/Pyodide because they use normal\nPython packages.\n\n::: {#8f1b8e72 .cell execution_count=1}\n``` {.python .cell-code}\nfrom edumath.linear_algebra import dot_product, vector_add, vector_norm\n\nu = [2, 1]\nv = [-1, 2]\n\nprint(vector_add(u, v))\nprint(vector_norm(u))\nprint(dot_product(u, v))\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n[1. 3.]\n2.23606797749979\n0.0\n```\n:::\n:::\n\n\n::: {#0cef1326 .cell execution_count=2}\n``` {.python .cell-code}\nimport sympy as sp\n\nu = sp.Matrix([2, 1])\nv = sp.Matrix([-1, 2])\n\nprint(u + v)\nprint(sp.sqrt(u.dot(u)))\nprint(u.dot(v))\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nMatrix([[1], [3]])\nsqrt(5)\n0\n```\n:::\n:::\n\n\n",
    "supporting": ["vectors_files/figure-html"],
    "filters": [],
    "includes": {}
  }
}
