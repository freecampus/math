---
title: Installation
---

## Base install

```bash
pip install osl-edu-math
```

The PyPI distribution is `osl-edu-math`; the import package remains
`edumath`.

## Development install

```bash
conda env create -f conda/dev.yaml
conda activate edumath
poetry config virtualenvs.create false
poetry install --extras "dev"
```

Run quality checks through Makim:

```bash
makim tests.linter
makim tests.unit
makim package.build
makim docs.build
```
